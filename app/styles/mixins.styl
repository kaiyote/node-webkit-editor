toolbar(color1, color2)
  z-index 999
  display flex
  align-items center
  justify-content flex-start
  background linear-gradient(to left, color2, mix(color2, #888, 90%))
  color color1
  transform translateZ(0)